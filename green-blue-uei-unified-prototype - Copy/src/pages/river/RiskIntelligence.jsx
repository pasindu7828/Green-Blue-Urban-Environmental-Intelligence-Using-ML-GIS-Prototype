import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import MetricCard from '../../components/shared/MetricCard'
import Badge from '../../components/ui/Badge'
import { riverSections } from '../../data/riverData'
const tone=r=>r==='Very High'?'red':r==='High'?'orange':r==='Medium'?'amber':'green'
export default function RiskIntelligence(){const importance=[['Riverbank movement',91],['Active channel width change',78],['Vegetation buffer loss',84],['Built-up encroachment',94],['Road proximity',73],['Distance to buildings',69],['Buffer condition',82]];const methods=[['GIS Risk Scoring',.88],['Random Forest',.91],['XGBoost',.93],['Decision Tree',.81],['KMeans Clustering',.76]];return <div><div className="mb-5"><h1 className="page-title">Riverbank encroachment risk intelligence</h1><p className="page-subtitle">Risk classes derived from riverbank movement, vegetation loss, built-up encroachment, road proximity, and buffer condition.</p></div><div className="data-note"><b>Prototype values are mock/demo outputs for proposal demonstration.</b> They are not final engineering risk measurements.</div><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard title="Low Risk" value="1 section" tone="green" compact/><MetricCard title="Medium Risk" value="2 sections" tone="amber" compact/><MetricCard title="High Risk" value="3 sections" tone="orange" compact/><MetricCard title="Very High Risk" value="2 sections" tone="red" compact/></div><div className="mt-5 grid gap-5 lg:grid-cols-2"><div className="card-pad"><h2 className="section-title">Feature importance panel</h2><div className="mt-4 space-y-3">{importance.map(([name,v])=><div key={name}><div className="flex justify-between text-[11px]"><span>{name}</span><b>{v}%</b></div><div className="mt-1.5 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-sky-600" style={{width:`${v}%`}}/></div></div>)}</div></div><div className="card-pad"><h2 className="section-title">Model / method comparison</h2><p className="text-[10px] text-slate-400">Demo values only — method choice depends on available validated data.</p><div className="mt-4 h-[280px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={methods.map(([model,score])=>({model,score}))}><CartesianGrid/><XAxis dataKey="model" angle={-15} textAnchor="end" height={60}/><YAxis domain={[0,1]}/><Tooltip/><Bar dataKey="score" fill="#0E7C7B" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></div></div><div className="mt-5 table-shell"><div className="border-b border-slate-100 px-5 py-4"><h2 className="section-title">Risk table</h2></div><div className="overflow-x-auto"><table className="table-base"><thead><tr><th>Section</th><th>Movement</th><th>Vegetation Loss</th><th>Encroachment</th><th>Buffer Condition</th><th>Risk Class</th><th>Protection Priority</th><th>Recommendation</th></tr></thead><tbody>{riverSections.map(s=><tr key={s.sectionId}><td className="font-semibold">{s.sectionId} · {s.sectionName}</td><td>{s.riverbankMovementRate} m/yr</td><td>{s.vegetationBufferLoss}%</td><td>{s.builtUpEncroachment}/100</td><td>{s.bufferCondition}</td><td><Badge variant={tone(s.riskClass)}>{s.riskClass}</Badge></td><td>{s.protectionPriority}</td><td className="min-w-[280px]">{s.recommendation}</td></tr>)}</tbody></table></div></div></div>}
