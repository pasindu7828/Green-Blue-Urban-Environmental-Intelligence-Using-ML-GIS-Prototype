import { useState } from 'react'
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import Slider from '../../components/ui/Slider'
import Toggle from '../../components/ui/Toggle'
import Badge from '../../components/ui/Badge'
export default function Scenario(){const [veg,setVeg]=useState(35),[enc,setEnc]=useState(25),[buffer,setBuffer]=useState(30),[monitor,setMonitor]=useState(40),[strict,setStrict]=useState(true),[control,setControl]=useState(true),[movement,setMovement]=useState(true),[restore,setRestore]=useState(true);const risk=Math.max(8,Math.round(78-veg*.22-enc*.17-buffer*.16-monitor*.08-(strict?5:0)-(control?5:0)));const improvement=Math.min(100,Math.round(42+veg*.55+(restore?12:0)));const pressure=Math.max(0,Math.round(76-enc*.55-buffer*.2-(control?12:0)));const protection=Math.min(100,Math.round(50+buffer*.45+monitor*.25+(movement?9:0)));const chart=[{name:'Current risk',value:78},{name:'Scenario risk',value:risk}];return <div><div className="mb-5"><h1 className="page-title">River corridor protection scenario simulator</h1><p className="page-subtitle">Explore how buffer restoration and development-control scenarios may reduce riverbank risk.</p></div><div className="grid gap-5 xl:grid-cols-[.8fr_1.2fr]"><div className="card-pad"><h2 className="section-title">Scenario inputs</h2><div className="mt-5 space-y-6"><Slider label="Increase vegetation buffer restoration" value={veg} onChange={setVeg}/><Slider label="Reduce built-up encroachment" value={enc} onChange={setEnc}/><Slider label="Increase river buffer protection" value={buffer} onChange={setBuffer}/><Slider label="Improve monitoring priority" value={monitor} onChange={setMonitor}/></div><div className="mt-6 space-y-2"><Toggle checked={strict} onChange={setStrict} label="Apply strict 30 m buffer protection"/><Toggle checked={control} onChange={setControl} label="Apply development-control zone"/><Toggle checked={movement} onChange={setMovement} label="Prioritize high-movement sections"/><Toggle checked={restore} onChange={setRestore} label="Strengthen vegetation buffer restoration"/></div></div><div className="card-pad"><div className="flex items-center justify-between"><h2 className="section-title">Live demo outputs</h2><Badge variant="amber">Mock scenario</Badge></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="soft-panel"><div className="text-[10px] text-slate-400">Predicted risk reduction</div><div className="mt-1 text-2xl font-bold text-emerald-600">-{78-risk} pts</div></div><div className="soft-panel"><div className="text-[10px] text-slate-400">Protection priority score</div><div className="mt-1 text-2xl font-bold">{protection}/100</div></div><div className="soft-panel"><div className="text-[10px] text-slate-400">Vegetation buffer improvement</div><div className="mt-1 text-2xl font-bold">{improvement}/100</div></div><div className="soft-panel"><div className="text-[10px] text-slate-400">Encroachment pressure change</div><div className="mt-1 text-2xl font-bold">{pressure}/100</div></div></div><div className="mt-4 h-[260px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={chart}><CartesianGrid/><XAxis dataKey="name"/><YAxis domain={[0,100]}/><Tooltip/><Bar dataKey="value" fill="#2B6CB0" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div><div className="mt-4 rounded-xl border border-sky-100 bg-sky-50 p-4 text-sm text-sky-800"><b>Planning recommendation:</b> {risk<45?'Prioritize vegetation buffer restoration and restrict high-impact development near high-movement sections.':'Monitor high-movement sections and require environmental review before new construction.'}</div></div></div><div className="data-note mt-5">These are demo scenario outputs generated from mock data for proposal demonstration only.</div></div>}
